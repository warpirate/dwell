<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark variant. Same shape as the light theme, dark platform parent. -->
    <style name="Theme.Dwell" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/dwell_window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
