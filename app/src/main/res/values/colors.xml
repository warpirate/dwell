<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Pre-Compose window background (light). Matches token bg #FAFAF8 so the
         launch window never flashes a color the theme doesn't use. -->
    <color name="dwell_window_background">#FAFAF8</color>
</resources>
