<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Firebase needs network. INTERNET + connectivity state are normal
         install-time permissions. Runtime permissions (calendar, notifications)
         are requested contextually in later phases, never declared early. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Applying a wallpaper. Normal install-time permission, no runtime prompt. -->
    <uses-permission android:name="android.permission.SET_WALLPAPER" />

    <application
        android:name=".DwellApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dwell">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Dwell">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".widget.clock.ClockWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/clock_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.clock.WidgetPinReceiver"
            android:exported="false" />

        <receiver
            android:name=".widget.poster.PosterClockWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/poster_clock_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.poster.PosterPinReceiver"
            android:exported="false" />

        <receiver
            android:name=".widget.date.DateWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/date_widget_info" />
        </receiver>

        <activity
            android:name=".ui.widgetconfig.WidgetConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.Dwell">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.widgetconfig.PosterWeatherConfigActivity"
            android:exported="false"
            android:theme="@style/Theme.Dwell" />

        <activity
            android:name=".ui.paywall.PaywallActivity"
            android:exported="false"
            android:theme="@style/Theme.Dwell" />
    </application>

</manifest>
