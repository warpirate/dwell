package com.dwell.app.ui.theme

import androidx.compose.ui.unit.dp

/**
 * The 8dp spacing grid. Use these instead of raw dp literals so spacing stays
 * consistent across every screen (design system).
 */
object DwellSpacing {
    val xs = 4.dp
    val sm = 8.dp
    val md = 12.dp
    val lg = 16.dp
    val xl = 20.dp
    val xxl = 24.dp
    val section = 32.dp

    /** Page horizontal padding, matching the design mocks' gutter. */
    val screenGutter = 22.dp
}
